import arrowIcon from "../../assets/slider/arrow-left.svg";

const ArrowLeft: React.FC = () => {
    return (
        <div className="arrow arrow--left">
            <img src={arrowIcon} alt="" />
        </div>
    );
};

export default ArrowLeft;
